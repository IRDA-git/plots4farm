% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plan_parcelle.R
\name{plan.parcelle}
\alias{plan.parcelle}
\title{Découpe une parcelle en sous-polygones numérotés}
\usage{
plan.parcelle(
  points,
  nom.parcelle,
  id.start = 101,
  orientation = c("X", "Y"),
  inv.orientation = FALSE,
  nx = 8,
  ny = 1,
  inset = 0
)
}
\arguments{
\item{points}{Un objet \code{sf} de géométries POINT, avec une colonne \code{Name}
contenant les identifiants des parcelles.}

\item{nom.parcelle}{Chaîne de caractères, nom exact de la parcelle à filtrer (e.g. "C200-1").}

\item{id.start}{Entier, première valeur d'ID à attribuer aux cellules.}

\item{orientation}{Caractère, "Y" (nord→sud) ou "X" (ouest→est).}

\item{inv.orientation}{Booléen ; si TRUE, inverse l'ordre de tri.}

\item{nx}{Entier ≥1, nombre de colonnes dans la grille.}

\item{ny}{Entier ≥1, nombre de lignes dans la grille.}

\item{inset}{Nombre ≥0, distance en mètres pour réduire chaque cellule (buffer négatif).}
}
\value{
Un \code{sf} de POLYGON contenant :
\describe{
\item{geometry}{la géométrie de chaque cellule}
\item{id}{ID en caractère, débutant à \code{id.start}}
}
}
\description{
Filtre un objet \code{sf} de points selon le nom de parcelle,
assemble les points en polygone, crée une grille alignée,
réduit éventuellement chaque cellule (inset), ordonne les cellules
(nord→sud ou ouest→est), et assigne des identifiants séquentiels.
}
\examples{
\dontrun{
library(sf)
pts <- read_sf("plots_C200.shp")
grille <- plan.parcelle(
  points = pts,
  nom.parcelle = "C200-1",
  id.start = 101,
  orientation = "Y",
  inv.orientation = FALSE,
  nx = 8,
  ny = 1,
  inset = 0.5
)
plot(grille["id"])
}
}
